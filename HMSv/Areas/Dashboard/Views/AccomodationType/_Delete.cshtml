
@model HMSv.Areas.Dashboard.ViewModels.AccomodationTypesCreateModel

@if (Model != null)
{
    <div class="modal-content">
        <div class="modal-header bg-danger text-white">
            <h5 class="modal-title">Delete Accomodation Type</h5>
            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span class="text-white" aria-hidden="true">&times;</span>
            </button>
        </div>
        <div class="modal-body">
            <div class="alert alert-danger" role="alert">
                <h6>Do you really want to delete this entry?</h6>
            </div>
            <form id="deleteForm">
                <div class="form-group">
                    <label for="Name">AccomodationType Name</label>
                    <input type="text" class="form-control" id="Name" name="Name" value="@Model.Name" disabled="disabled">
                    @Html.HiddenFor(a => a.ID)
                    @Html.AntiForgeryToken()
                </div>
            </form>
        </div>
        @*<div class="errorMsg">

            </div>*@
        <div class="modal-footer">
            <button type="button" class="btn btn-danger" data-dismiss="modal">No</button>
            <button id="deleteBtn" type="button" class="btn btn-primary">Yes</button>

        </div>
    </div>
}

<script>
    $("#deleteBtn").click(function () {
        $.ajax({
            url: '@Url.Action("Delete","AccomodationType")',
            type: 'post',
            data: $("deleteForm").serialize()
        })
        .done(function (response) {
            $("#actionModal").modal("hide");

            $("#tableContainer").html(response);
            //location.reload();
            //if (response.Success)
            //{
            //   // location.reload();
            //}
            //else {
            //    $(".errorMsg").html(response.Message);
            //}

        })
    });
</script>

